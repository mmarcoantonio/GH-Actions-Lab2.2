name: Build V1
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        uses: npm ci
      - name: Lint
        uses: npm run lint
      - name: Test code
        uses: npm run test
      - name: Build code
        uses: npm run build
      - name: Deply code
        uses: echo "Deploying..."